"use strict";function installGeoJsonSource(id,data){const existing=map.getSource(id);if(existing)existing.setData(data);else map.addSource(id,{type:"geojson",data});}function addLayerIfMissing(layer,beforeId){if(!map.getLayer(layer.id))map.addLayer(layer,beforeId);}function installBaseLayers(){installGeoJsonSource("survey-coverage",state.coverage);addLayerIfMissing({id:"survey-coverage-fill",type:"fill",source:"survey-coverage",paint:{"fill-color":"#0f5d6c","fill-opacity":0.1},filter:["==",["get","coverage_id"],"__none__"],});addLayerIfMissing({id:"survey-coverage-line",type:"line",source:"survey-coverage",paint:{"line-color":"#0f5d6c","line-width":2.4,"line-dasharray":[2,1.5],},filter:["==",["get","coverage_id"],"__none__"],});map.on("click","survey-coverage-fill",(event)=>{const properties=event.features?.[0]?.properties;if(!properties)return;new maplibregl.Popup().setLngLat(event.lngLat).setHTML(`
          <h3>${escapeHtml(properties.source_title)}</h3>
          <p><strong>Survey date:</strong> ${escapeHtml(properties.survey_date)}</p>
          <p>${escapeHtml(properties.notice)}</p>
        `).addTo(map);});map.on("mouseenter","survey-coverage-fill",()=>{map.getCanvas().style.cursor="pointer";});map.on("mouseleave","survey-coverage-fill",()=>{map.getCanvas().style.cursor="";});}async function installCuspLayer(){$("cusp-status").textContent="Loading…";try{const geojson=await fetchJson(CUSP_QUERY_URL,{timeoutMs:20000});installGeoJsonSource("modern-cusp",geojson);addLayerIfMissing({id:"modern-cusp-line",type:"line",source:"modern-cusp",paint:{"line-color":"#006de6","line-width":["interpolate",["linear"],["zoom"],10,1.2,15,3],"line-opacity":0.9,},});state.cuspLoaded=true;$("cusp-status").textContent=`${geojson.features?.length || 0} segments`;}catch(error){state.cuspLoaded=false;$("cusp-status").textContent="Unavailable";}updateLayerVisibility();}function removeActiveEvidence(){if(!state.activeEvidence)return;if(map.getLayer(state.activeEvidence.layerId))map.removeLayer(state.activeEvidence.layerId);if(map.getSource(state.activeEvidence.sourceId))map.removeSource(state.activeEvidence.sourceId);state.activeEvidence=null;}function evidenceLayerIds(identity){const safe=identity.replace(/[^a-z0-9-]/gi,"-").toLowerCase();return{sourceId:`evidence-source-${safe}`,layerId:`evidence-layer-${safe}`};}function installEvidenceOverlay(overlay){removeActiveEvidence();if(!overlay||overlay.type==="modern-line")return;const ids=evidenceLayerIds(overlay.identity);const beforeId=map.getStyle().layers.find((layer)=>layer.type==="symbol")?.id;if(overlay.type==="xyz"){map.addSource(ids.sourceId,{type:"raster",tiles:overlay.tiles,tileSize:overlay.tileSize||256,minzoom:overlay.minzoom||0,maxzoom:overlay.maxzoom||22,attribution:overlay.attribution||"",});}else if(overlay.type==="arcgis-image"){const[west,south,east,north]=overlay.bbox;map.addSource(ids.sourceId,{type:"image",url:periodUtils.buildArcGisExportUrl(overlay),coordinates:[[west,north],[east,north],[east,south],[west,south],],});}else{throw new Error(`Unsupported evidence overlay type: ${overlay.type}`);}map.addLayer({id:ids.layerId,type:"raster",source:ids.sourceId,paint:{"raster-opacity":Number($("evidence-opacity").value)/100,"raster-fade-duration":0,"raster-resampling":"linear",},},beforeId);state.activeEvidence={...ids,identity:overlay.identity,type:overlay.type};}function setLayerVisibility(layerId,visible){if(map.getLayer(layerId)){map.setLayoutProperty(layerId,"visibility",visible?"visible":"none");}}function updateLayerVisibility(){if(!state.mapReady||!state.periods.length)return;const period=state.periods[state.index];const evidenceVisible=$("toggle-evidence").checked;const modernVisible=period.kind==="modern"?evidenceVisible:$("toggle-modern-shoreline").checked;if(state.activeEvidence)setLayerVisibility(state.activeEvidence.layerId,evidenceVisible);setLayerVisibility("modern-cusp-line",modernVisible);setLayerVisibility("survey-coverage-fill",$("toggle-coverage").checked);setLayerVisibility("survey-coverage-line",$("toggle-coverage").checked);}function setEvidenceOpacity(value){const opacity=Number(value)/100;$("evidence-opacity-output").textContent=`${Math.round(opacity * 100)}%`;if(state.activeEvidence&&map.getLayer(state.activeEvidence.layerId)){map.setPaintProperty(state.activeEvidence.layerId,"raster-opacity",opacity);}const period=state.periods[state.index];if(period?.kind==="modern"&&map.getLayer("modern-cusp-line")){map.setPaintProperty("modern-cusp-line","line-opacity",opacity);}}function updateCoverageFilter(period){const filter=period.coverageId?["==",["get","coverage_id"],period.coverageId]:["==",["get","coverage_id"],"__none__"];if(map.getLayer("survey-coverage-fill"))map.setFilter("survey-coverage-fill",filter);if(map.getLayer("survey-coverage-line"))map.setFilter("survey-coverage-line",filter);}function updateEvidenceControls(period){const overlay=period.overlay;const hasRaster=overlay.type!=="modern-line";$("toggle-evidence").disabled=false;$("evidence-opacity").disabled=false;$("evidence-label").textContent=period.kind==="modern"?"Selected modern shoreline evidence":"Selected historical evidence overlay";$("evidence-opacity-label").textContent=hasRaster?"Evidence opacity":"Shoreline opacity";const opacity=Math.round((overlay.defaultOpacity??0.75)*100);$("evidence-opacity").value=String(opacity);$("evidence-opacity-output").textContent=`${opacity}%`;$("toggle-modern-shoreline").disabled=period.kind==="modern";}function renderPeriodText(period){$("period-kicker").textContent=period.kicker;$("period-title").textContent=period.title;$("period-summary").textContent=period.summary;$("period-confidence").textContent=period.confidenceLabel;$("data-status").textContent=period.dataStatus;$("overlay-status").textContent=period.overlay.title;$("evidence-count").textContent=String(period.evidenceCount||1);$("period-highlights").replaceChildren(...period.highlights.map((text)=>{const item=document.createElement("li");item.textContent=text;return item;}));renderSourceLinks($("period-sources"),period.sourceIds);const distinct=period.kind==="aerial"?`This map state is generated only from NOAA catalog rasters dated ${period.shortLabel}; it does not reuse another year’s overlay.`:period.kind==="chart"?"This map state uses the 1907 chart only and is not reused under another date.":"This map state is the modern shoreline reference, not a historical overlay.";$("data-note").textContent=distinct;}function renderPeriod(){if(!state.periods.length)return;const period=state.periods[state.index];renderPeriodText(period);updateEvidenceControls(period);if(!state.mapReady)return;try{clearMapMessage();installEvidenceOverlay(period.overlay);updateCoverageFilter(period);setEvidenceOpacity($("evidence-opacity").value);updateLayerVisibility();}catch(error){showMapMessage(`The ${period.shortLabel} evidence layer could not be displayed: ${error.message}`);}}
