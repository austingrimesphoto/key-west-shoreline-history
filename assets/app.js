(()=>{"use strict";const $=(id)=>document.getElementById(id);const periodUtils=window.KeyWestPeriodUtils;if(!periodUtils)throw new Error("Period utilities failed to load.");const TRUMBO_BOUNDS=[[-81.814,24.543],[-81.770,24.571]];const BASE_STYLE="https://tiles.openfreemap.org/styles/liberty";const CUSP_QUERY_URL="https://services.arcgis.com/rD2ylXRs80UroD90/ArcGIS/rest/services/NOAA_Coastal_Shoreline/FeatureServer/0/query"+"?where=1%3D1"+"&geometry=-81.84%2C24.46%2C-81.68%2C24.73"+"&geometryType=esriGeometryEnvelope"+"&inSR=4326"+"&spatialRel=esriSpatialRelIntersects"+"&outFields=FEATURE_ID%2CSOURCE_ID%2CSRC_DATE%2CHOR_ACC%2CATTRIBUTE%2CDATA_SOURC%2CEX_METH%2CSRC_CITA"+"&returnGeometry=true"+"&outSR=4326"+"&resultRecordCount=2000"+"&f=geojson";const state={manifest:null,periods:[],sources:[],coverage:null,index:0,mapReady:false,activeEvidence:null,cuspLoaded:false,aerialDiscovery:{status:"pending",years:0,frames:0},};const map=new maplibregl.Map({container:"map",style:BASE_STYLE,center:[-81.7736,24.5557],zoom:12,minZoom:10,maxZoom:18,attributionControl:true,maplibreLogo:true,});map.addControl(new maplibregl.NavigationControl({visualizePitch:true}),"top-right");map.addControl(new maplibregl.FullscreenControl(),"top-right");map.addControl(new maplibregl.ScaleControl({maxWidth:120,unit:"imperial"}),"bottom-right");function escapeHtml(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");}async function fetchJson(url,{timeoutMs=15000,cache="no-cache"}={}){const controller=new AbortController();const timer=window.setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetch(url,{cache,signal:controller.signal});if(!response.ok)throw new Error(`${response.status} ${response.statusText}`);const data=await response.json();if(data?.error){throw new Error(data.error.message||"The remote GIS service returned an error.");}return data;}finally{window.clearTimeout(timer);}}function showMapMessage(text){const node=$("map-message");node.textContent=text;node.hidden=false;}function clearMapMessage(){$("map-message").hidden=true;}function studyFitBounds(){const[west,south,east,north]=state.manifest.studyBounds;return[[west,south],[east,north]];}function fit(bounds,duration=700){map.fitBounds(bounds,{padding:{top:42,right:42,bottom:42,left:42},duration,});}function sourceById(id){return state.sources.find((source)=>source.id===id);}function renderSourceLinks(container,sourceIds,className="source-link"){container.replaceChildren(...sourceIds.map(sourceById).filter(Boolean).map((source)=>{const link=document.createElement("a");link.className=className;link.href=source.url;link.target="_blank";link.rel="noreferrer";link.innerHTML=`
            <span class="source-title">${escapeHtml(source.title)}</span>
            <span class="source-meta">${escapeHtml(source.institution)} · ${escapeHtml(source.coverage)}</span>
          `;return link;}));}function buildSourceCatalog(){const catalog=$("source-catalog");catalog.replaceChildren(...state.sources.map((source)=>{const link=document.createElement("a");link.className="source-card";link.href=source.url;link.target="_blank";link.rel="noreferrer";link.innerHTML=`
          <span class="source-title">${escapeHtml(source.title)}</span>
          <span class="source-meta">${escapeHtml(source.institution)} · ${escapeHtml(source.coverage)}</span>
          <span class="source-meta">${escapeHtml(source.use)}</span>
        `;return link;}));}function buildMilestones(){const container=$("milestone-list");container.replaceChildren(...state.manifest.milestones.map((milestone)=>{const article=document.createElement("article");article.className="milestone-card";article.innerHTML=`
          <div class="milestone-year">${escapeHtml(milestone.year)}</div>
          <div>
            <h4>${escapeHtml(milestone.title)}</h4>
            <p>${escapeHtml(milestone.status)}</p>
            <div class="milestone-sources"></div>
          </div>
        `;renderSourceLinks(article.querySelector(".milestone-sources"),milestone.sourceIds,"source-chip");return article;}));}function aerialPeriodFromGroup(year,records,config){const objectIds=[...new Set(records.map((record)=>Number(record.OBJECTID)).filter(Number.isFinite))].slice(0,config.maxRasterIdsPerYear);const missions=[...new Set(records.map((record)=>record.Mission).filter((value)=>value!==null&&value!==undefined))];const sensors=[...new Set(records.map((record)=>record.Sensor).filter(Boolean))];const resolutions=records.map((record)=>Number(record.Resolution)).filter((value)=>Number.isFinite(value)&&value>0);const resolutionText=resolutions.length?`${Math.min(...resolutions).toFixed(2)}–${Math.max(...resolutions).toFixed(2)} m listed resolution`:"Resolution varies by frame";return{id:`aerial-${year}`,shortLabel:String(year),sortYear:year,kind:"aerial",kicker:"NOAA georeferenced historical aerial imagery",title:`${year} aerial coverage of Key West`,summary:`NOAA’s live image catalog returned ${objectIds.length} distinct historical aerial frame${objectIds.length === 1 ? "" : "s"} from ${year} intersecting the Key West study area. This is a different source mosaic from every other year shown on the timeline.`,confidenceLabel:"Georeferenced aerial",dataStatus:`${objectIds.length} NOAA frame${objectIds.length === 1 ? "" : "s"}`,evidenceCount:objectIds.length,highlights:[missions.length?`Catalog mission identifiers: ${missions.join(", ")}.`:"NOAA catalog mission metadata is incomplete for these frames.",sensors.length?`Sensors represented: ${sensors.join(", ")}.`:"Sensor metadata is not listed for every frame.",`${resolutionText}. NOAA warns that these historical photographs are georeferenced but not orthorectified.`,],sourceIds:[config.sourceId],focusBounds:studyFitBounds(),overlay:{type:"arcgis-image",identity:`${config.identityPrefix}-${year}`,title:`${year} NOAA historical aerial mosaic`,service:config.service,rasterIds:objectIds,bbox:state.manifest.studyBounds,imageSize:config.imageSize,attribution:`NOAA historical aerial imagery, ${year}`,defaultOpacity:0.78,},aerialMetadata:{missions,sensors,resolutionText},};}async function discoverAerialPeriods(config){const queryUrl=periodUtils.buildAerialQueryUrl(config,state.manifest.studyBounds);try{const data=await fetchJson(queryUrl,{timeoutMs:20000});const periods=periodUtils.groupAerialAttributes(data.features||[],config).map(([year,records])=>aerialPeriodFromGroup(year,records,config)).filter((period)=>period.overlay.rasterIds.length>0);state.aerialDiscovery={status:"loaded",years:periods.length,frames:periods.reduce((sum,period)=>sum+period.evidenceCount,0),};return periods;}catch(error){state.aerialDiscovery={status:"unavailable",years:0,frames:0,error:error.name==="AbortError"?"The NOAA catalog request timed out.":error.message,};return[];}}function periodSortValue(period){if(Number.isFinite(period.sortYear))return period.sortYear;const parsed=Number.parseInt(period.id,10);return Number.isFinite(parsed)?parsed:9999;}function buildTimeline(){const slider=$("period-slider");slider.max=String(Math.max(0,state.periods.length-1));slider.disabled=state.periods.length<2;const labels=$("timeline-labels");labels.innerHTML=state.periods.length?`<span>${escapeHtml(state.periods[0].shortLabel)}</span><span>${escapeHtml(state.periods.at(-1).shortLabel)}</span>`:"";const buttons=$("period-buttons");buttons.replaceChildren(...state.periods.map((period,index)=>{const button=document.createElement("button");button.type="button";button.className="period-button";button.textContent=period.shortLabel;button.setAttribute("aria-label",`Show ${period.title}`);button.addEventListener("click",()=>selectPeriod(index));return button;}));const note=$("availability-note");if(state.aerialDiscovery.status==="loaded"){note.textContent=`${state.aerialDiscovery.years} distinct NOAA aerial years were verified over Key West (${state.aerialDiscovery.frames} catalog frames). Only verified map states are shown.`;$("aerial-status").textContent=`${state.aerialDiscovery.years} years loaded`;}else{note.textContent="The NOAA aerial catalog could not be reached. No unverified aerial years were added; only fixed, proven map states are shown.";$("aerial-status").textContent="Unavailable";}}function updatePeriodButtons(){document.querySelectorAll(".period-button").forEach((button,index)=>{const active=index===state.index;button.classList.toggle("is-active",active);button.setAttribute("aria-current",active?"true":"false");});}function selectPeriod(index,updateUrl=true){state.index=Math.max(0,Math.min(index,state.periods.length-1));$("period-slider").value=String(state.index);updatePeriodButtons();if(updateUrl&&state.periods[state.index]){const url=new URL(window.location.href);url.searchParams.set("period",state.periods[state.index].id);history.replaceState({},"",url);}renderPeriod();}function installGeoJsonSource(id,data){const existing=map.getSource(id);if(existing)existing.setData(data);else map.addSource(id,{type:"geojson",data});}function addLayerIfMissing(layer,beforeId){if(!map.getLayer(layer.id))map.addLayer(layer,beforeId);}function installBaseLayers(){installGeoJsonSource("survey-coverage",state.coverage);addLayerIfMissing({id:"survey-coverage-fill",type:"fill",source:"survey-coverage",paint:{"fill-color":"#0f5d6c","fill-opacity":0.1},filter:["==",["get","coverage_id"],"__none__"],});addLayerIfMissing({id:"survey-coverage-line",type:"line",source:"survey-coverage",paint:{"line-color":"#0f5d6c","line-width":2.4,"line-dasharray":[2,1.5],},filter:["==",["get","coverage_id"],"__none__"],});map.on("click","survey-coverage-fill",(event)=>{const properties=event.features?.[0]?.properties;if(!properties)return;new maplibregl.Popup().setLngLat(event.lngLat).setHTML(`
          <h3>${escapeHtml(properties.source_title)}</h3>
          <p><strong>Survey date:</strong> ${escapeHtml(properties.survey_date)}</p>
          <p>${escapeHtml(properties.notice)}</p>
        `).addTo(map);});map.on("mouseenter","survey-coverage-fill",()=>{map.getCanvas().style.cursor="pointer";});map.on("mouseleave","survey-coverage-fill",()=>{map.getCanvas().style.cursor="";});}async function installCuspLayer(){$("cusp-status").textContent="Loading…";try{const geojson=await fetchJson(CUSP_QUERY_URL,{timeoutMs:20000});installGeoJsonSource("modern-cusp",geojson);addLayerIfMissing({id:"modern-cusp-line",type:"line",source:"modern-cusp",paint:{"line-color":"#006de6","line-width":["interpolate",["linear"],["zoom"],10,1.2,15,3],"line-opacity":0.9,},});state.cuspLoaded=true;$("cusp-status").textContent=`${geojson.features?.length || 0} segments`;}catch(error){state.cuspLoaded=false;$("cusp-status").textContent="Unavailable";}updateLayerVisibility();}function removeActiveEvidence(){if(!state.activeEvidence)return;if(map.getLayer(state.activeEvidence.layerId))map.removeLayer(state.activeEvidence.layerId);if(map.getSource(state.activeEvidence.sourceId))map.removeSource(state.activeEvidence.sourceId);state.activeEvidence=null;}function evidenceLayerIds(identity){const safe=identity.replace(/[^a-z0-9-]/gi,"-").toLowerCase();return{sourceId:`evidence-source-${safe}`,layerId:`evidence-layer-${safe}`};}function installEvidenceOverlay(overlay){removeActiveEvidence();if(!overlay||overlay.type==="modern-line")return;const ids=evidenceLayerIds(overlay.identity);const beforeId=map.getStyle().layers.find((layer)=>layer.type==="symbol")?.id;if(overlay.type==="xyz"){map.addSource(ids.sourceId,{type:"raster",tiles:overlay.tiles,tileSize:overlay.tileSize||256,minzoom:overlay.minzoom||0,maxzoom:overlay.maxzoom||22,attribution:overlay.attribution||"",});}else if(overlay.type==="arcgis-image"){const[west,south,east,north]=overlay.bbox;map.addSource(ids.sourceId,{type:"image",url:periodUtils.buildArcGisExportUrl(overlay),coordinates:[[west,north],[east,north],[east,south],[west,south],],});}else{throw new Error(`Unsupported evidence overlay type: ${overlay.type}`);}map.addLayer({id:ids.layerId,type:"raster",source:ids.sourceId,paint:{"raster-opacity":Number($("evidence-opacity").value)/100,"raster-fade-duration":0,"raster-resampling":"linear",},},beforeId);state.activeEvidence={...ids,identity:overlay.identity,type:overlay.type};}function setLayerVisibility(layerId,visible){if(map.getLayer(layerId)){map.setLayoutProperty(layerId,"visibility",visible?"visible":"none");}}function updateLayerVisibility(){if(!state.mapReady||!state.periods.length)return;const period=state.periods[state.index];const evidenceVisible=$("toggle-evidence").checked;const modernVisible=period.kind==="modern"?evidenceVisible:$("toggle-modern-shoreline").checked;if(state.activeEvidence)setLayerVisibility(state.activeEvidence.layerId,evidenceVisible);setLayerVisibility("modern-cusp-line",modernVisible);setLayerVisibility("survey-coverage-fill",$("toggle-coverage").checked);setLayerVisibility("survey-coverage-line",$("toggle-coverage").checked);}function setEvidenceOpacity(value){const opacity=Number(value)/100;$("evidence-opacity-output").textContent=`${Math.round(opacity * 100)}%`;if(state.activeEvidence&&map.getLayer(state.activeEvidence.layerId)){map.setPaintProperty(state.activeEvidence.layerId,"raster-opacity",opacity);}const period=state.periods[state.index];if(period?.kind==="modern"&&map.getLayer("modern-cusp-line")){map.setPaintProperty("modern-cusp-line","line-opacity",opacity);}}function updateCoverageFilter(period){const filter=period.coverageId?["==",["get","coverage_id"],period.coverageId]:["==",["get","coverage_id"],"__none__"];if(map.getLayer("survey-coverage-fill"))map.setFilter("survey-coverage-fill",filter);if(map.getLayer("survey-coverage-line"))map.setFilter("survey-coverage-line",filter);}function updateEvidenceControls(period){const overlay=period.overlay;const hasRaster=overlay.type!=="modern-line";$("toggle-evidence").disabled=false;$("evidence-opacity").disabled=false;$("evidence-label").textContent=period.kind==="modern"?"Selected modern shoreline evidence":"Selected historical evidence overlay";$("evidence-opacity-label").textContent=hasRaster?"Evidence opacity":"Shoreline opacity";const opacity=Math.round((overlay.defaultOpacity??0.75)*100);$("evidence-opacity").value=String(opacity);$("evidence-opacity-output").textContent=`${opacity}%`;$("toggle-modern-shoreline").disabled=period.kind==="modern";}function renderPeriodText(period){$("period-kicker").textContent=period.kicker;$("period-title").textContent=period.title;$("period-summary").textContent=period.summary;$("period-confidence").textContent=period.confidenceLabel;$("data-status").textContent=period.dataStatus;$("overlay-status").textContent=period.overlay.title;$("evidence-count").textContent=String(period.evidenceCount||1);$("period-highlights").replaceChildren(...period.highlights.map((text)=>{const item=document.createElement("li");item.textContent=text;return item;}));renderSourceLinks($("period-sources"),period.sourceIds);const distinct=period.kind==="aerial"?`This map state is generated only from NOAA catalog rasters dated ${period.shortLabel}; it does not reuse another year’s overlay.`:period.kind==="chart"?"This map state uses the 1907 chart only and is not reused under another date.":"This map state is the modern shoreline reference, not a historical overlay.";$("data-note").textContent=distinct;}function renderPeriod(){if(!state.periods.length)return;const period=state.periods[state.index];renderPeriodText(period);updateEvidenceControls(period);if(!state.mapReady)return;try{clearMapMessage();installEvidenceOverlay(period.overlay);updateCoverageFilter(period);setEvidenceOpacity($("evidence-opacity").value);updateLayerVisibility();}catch(error){showMapMessage(`The ${period.shortLabel} evidence layer could not be displayed: ${error.message}`);}}function configureTabs(){const tabs=[...document.querySelectorAll('[role="tab"]')];tabs.forEach((tab)=>{tab.addEventListener("click",()=>{tabs.forEach((candidate)=>{const active=candidate===tab;candidate.classList.toggle("is-active",active);candidate.setAttribute("aria-selected",String(active));$(candidate.getAttribute("aria-controls")).hidden=!active;});});});}function configureControls(){$("period-slider").addEventListener("input",(event)=>selectPeriod(Number(event.target.value)));$("toggle-evidence").addEventListener("change",updateLayerVisibility);$("toggle-modern-shoreline").addEventListener("change",updateLayerVisibility);$("toggle-coverage").addEventListener("change",updateLayerVisibility);$("evidence-opacity").addEventListener("input",(event)=>setEvidenceOpacity(event.target.value));$("reset-view").addEventListener("click",()=>fit(studyFitBounds()));$("focus-trumbo").addEventListener("click",()=>fit(TRUMBO_BOUNDS));const dialog=$("about-dialog");$("open-about").addEventListener("click",()=>dialog.showModal());$("close-about").addEventListener("click",()=>dialog.close());dialog.addEventListener("click",(event)=>{if(event.target===dialog)dialog.close();});window.addEventListener("keydown",(event)=>{const tag=document.activeElement?.tagName;if(tag==="INPUT"||tag==="BUTTON")return;if(event.key==="ArrowLeft")selectPeriod(state.index-1);if(event.key==="ArrowRight")selectPeriod(state.index+1);});}async function initializeData(){try{$("availability-note").textContent="Checking NOAA’s catalog for distinct historical aerial years over Key West…";const[manifest,sources,coverage]=await Promise.all([fetchJson("./data/periods.json"),fetchJson("./data/sources.json"),fetchJson("./data/survey-coverage.geojson"),]);state.manifest=manifest;state.sources=sources.sources;state.coverage=coverage;buildSourceCatalog();buildMilestones();const discovered=await discoverAerialPeriods(manifest.aerialDiscovery);state.periods=periodUtils.enforceDistinctMapStates([...manifest.periods,...discovered].sort((a,b)=>periodSortValue(a)-periodSortValue(b)));buildTimeline();const requested=new URL(window.location.href).searchParams.get("period");const requestedIndex=state.periods.findIndex((period)=>period.id===requested);selectPeriod(requestedIndex>=0?requestedIndex:0,false);}catch(error){$("period-title").textContent="Project manifest unavailable";$("period-summary").textContent=error.message;showMapMessage(error.message);}}configureTabs();configureControls();const dataReady=initializeData();map.on("load",async()=>{state.mapReady=true;await dataReady;if(!state.manifest||!state.coverage)return;fit(studyFitBounds(),0);installBaseLayers();await installCuspLayer();renderPeriod();});map.on("error",(event)=>{const message=event?.error?.message||"";if(message&&!message.includes("Failed to fetch"))showMapMessage(message);});})();